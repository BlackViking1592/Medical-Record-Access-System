# MRAS Client

Simple React client that demonstrates registering, logging in, adding a JSON medical record, and viewing the file-based blockchain.

Setup:

1. cd client
2. npm install
3. REACT_APP_API=http://localhost:5000 npm start
